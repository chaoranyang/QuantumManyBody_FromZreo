bose-hubbard模型平均场自洽算法绘制定性相图

任务描述：

1、在平均场近似下，多体哈密顿量退化为单格点算符，其含有超流序参量phi=<gs| b |gs>。在单格点fock空间中构造哈密顿量矩阵H，自洽迭代求解方程
 phi=<gs( H(phi) ) | b | gs( H(phi) ) > （其中gs代表基态本征矢,b是玻色子湮灭算符）

2、遍历(J,mu)参数空间，算出每组参数的phi，以其是否大于0.01为超流/绝缘体相判据，绘制定性相图

================================================================================